import { Actions } from 'react-native-router-flux';
import data from '../Assets/JSON/users.json';

export function validUser(user,pass){
    console.log("Actions: userName :"+user+" & " +"Pass :"+pass)
}